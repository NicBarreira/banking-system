# banking-system
Implementation of a Java banking system via command line (CLI). The project focuses on the application of fundamental Object-Oriented Programming (OOP) concepts such as Encapsulation, Class Methods, and Flow Logic (switch-case and while loops).
